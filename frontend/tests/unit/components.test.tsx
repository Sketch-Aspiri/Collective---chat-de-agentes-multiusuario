import { describe, it } from 'vitest';

describe('MessageInput', () => {
  it.todo('calls onSend with trimmed content and clears the field');
});

describe('ChatWindow', () => {
  it.todo('renders the chat header and message thread');
});
