import { describe, it } from 'vitest';

describe('useChat', () => {
  it.todo('fetches and stores the chat list');
});

describe('useAgents', () => {
  it.todo('fetches and stores agents for a chat');
});
